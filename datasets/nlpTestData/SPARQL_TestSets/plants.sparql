PREFIX dbpedia: <http://dbpedia.org/ontology/>
PREFIX yago: <http://dbpedia.org/class/yago/>
CONSTRUCT {
  ?adReg  dbpedia:abstract ?abstract.
}
WHERE {?adReg a <http://dbpedia.org/ontology/Plant> .
    ?adReg dbpedia:abstract ?abstract .
    FILTER (lang(?abstract) = "en") .
}
LIMIT 1000